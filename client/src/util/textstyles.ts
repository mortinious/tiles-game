import { TextStyle } from "pixi.js";
import colors from "../../../common/colors.json";

export const TitleTextStyle = new TextStyle({
    fontSize: 60,
    fill: colors.text
})

export const LargeTextStyle = new TextStyle({
    fontSize: 40,
    fill: colors.text
})

export const MediumTextStyle = new TextStyle({
    fontSize: 30,
    fill: colors.text
})

export const SmallTextStyle = new TextStyle({
    fontSize: 20,
    fill: colors.text
})